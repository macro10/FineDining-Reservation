from get_res import getRes

getRes(0, 5, "6", "30", "", "", "", True)
# getRes(restaurant(int), pSize(int), hr(str), min(str), username(str), pswd(str), phone(str), complete(bool))

# Prime is 0, Alo is 1, Kazoku is 2